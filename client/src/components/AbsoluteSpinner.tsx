import React from 'react'
import { Spinner } from 'react-bootstrap'

const AbsoluteSpinner = () => {
  return <Spinner className="center-absolute" animation="border" />
}

export default AbsoluteSpinner
