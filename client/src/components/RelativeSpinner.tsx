import React from 'react'
import { Spinner } from 'react-bootstrap'

const RelativeSpinner = () => {
  return <Spinner className="center-relative" animation="border" />
}

export default RelativeSpinner
